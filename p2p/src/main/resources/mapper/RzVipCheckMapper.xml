<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gs.dao.RzVipCheckDAO">

    <!--新增信息-->
    <insert id="save" parameterType="com.gs.bean.RzVipCheck">
        <![CDATA[
          insert into t_rzvipcheck(uid, huid, isok, excute, date) values(#{uid}, #{huid}, #{isok}, #{excute}, #{date});
        ]]>
    </insert>

</mapper>